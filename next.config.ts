import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // node: built-in modullar (sqlite, path, fs) server build'da avtomatik
  // external bo'ladi. Qo'shimcha sozlash shart emas.
};

export default nextConfig;
